apiVersion: v1
kind: ConfigMap
metadata:
    name: skald-worker-config
    namespace: skald
    labels:
        app: skald-worker
data:
    # Skald API Configuration
    SKALD_BASE_URL: 'http://api-service:8000'
    SKALD_PROJECT_ID: '83dabf13-0c3e-41f0-8f6b-75a817cd1e25'

    # Jira Configuration
    JIRA_JQL_FILTER: 'TYPE IN (인시던트, 장애) AND updated >= -1d ORDER BY updated DESC'
    JIRA_POLL_INTERVAL_MINUTES: '10'
    JIRA_ENABLED: 'true'
    JIRA_URL: ''

    # Docs Configuration
    DOCS_POLL_INTERVAL_MINUTES: '30'
    DOCS_ENABLED: 'true'

    # Notion Configuration
    NOTION_ENABLED: 'false'
    NOTION_ROOT_PAGE_ID: 'replace-with-notion-root-page-id'
    NOTION_SYNC_CRON_HOUR: '1'
    NOTION_SYNC_CRON_MINUTE: '0'
    NOTION_MAX_DEPTH: '5'
    NOTION_MAX_PAGES: '500'

    # Worker Configuration
    WORKER_CONCURRENCY: '4'
    LOG_LEVEL: 'INFO'
    LOG_FORMAT: 'json'

    # Server Configuration
    HOST: '0.0.0.0'
    PORT: '8080'
