apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: skald-worker-state
    namespace: skald
    labels:
        app: skald
        component: worker
        environment: production
spec:
    accessModes:
        - ReadWriteOnce
    resources:
        requests:
            storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
    name: skald-worker
    namespace: skald
    labels:
        app: skald
        component: worker
        environment: production
spec:
    replicas: 1
    strategy:
        type: Recreate
    selector:
        matchLabels:
            app: skald
            component: worker
    template:
        metadata:
            labels:
                app: skald
                component: worker
            annotations:
                prometheus.io/scrape: 'true'
                prometheus.io/port: '8080'
                prometheus.io/path: '/metrics'
        spec:
            serviceAccountName: skald-worker
            securityContext:
                runAsNonRoot: true
                runAsUser: 1000
                fsGroup: 1000
                fsGroupChangePolicy: OnRootMismatch
            containers:
                - name: worker
                  image: ghcr.io/jc01rho/skald-worker-v2:latest
                  imagePullPolicy: Always
                  ports:
                      - name: http
                        containerPort: 8080
                        protocol: TCP
                      - name: metrics
                        containerPort: 8080
                        protocol: TCP
                  envFrom:
                      - configMapRef:
                            name: skald-worker-config
                            optional: false
                      - secretRef:
                            name: skald-worker-secrets
                  resources:
                      requests:
                          memory: '256Mi'
                          cpu: '100m'
                      limits:
                          memory: '512Mi'
                          cpu: '500m'
                  startupProbe:
                      httpGet:
                          path: /health
                          port: http
                      periodSeconds: 10
                      timeoutSeconds: 5
                      failureThreshold: 180
                  livenessProbe:
                      httpGet:
                          path: /health
                          port: http
                      initialDelaySeconds: 10
                      periodSeconds: 30
                      timeoutSeconds: 10
                      failureThreshold: 3
                  readinessProbe:
                      httpGet:
                          path: /health
                          port: http
                      initialDelaySeconds: 5
                      periodSeconds: 10
                      timeoutSeconds: 5
                      failureThreshold: 3
                  securityContext:
                      allowPrivilegeEscalation: false
                      readOnlyRootFilesystem: true
                      capabilities:
                          drop:
                              - ALL
                  volumeMounts:
                      - name: tmp
                        mountPath: /tmp
                      - name: sync-state
                        mountPath: /var/lib/skald-worker
            volumes:
                - name: tmp
                  emptyDir: {}
                - name: sync-state
                  persistentVolumeClaim:
                      claimName: skald-worker-state
            affinity:
                nodeAffinity:
                    requiredDuringSchedulingIgnoredDuringExecution:
                        nodeSelectorTerms:
                            - matchExpressions:
                                  - key: kubernetes.io/hostname
                                    operator: NotIn
                                    values:
                                        - ml.node.k8s.sparrow.local
            restartPolicy: Always
